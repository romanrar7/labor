# вказування значень a та b
a = float(input("значення a (1 <= a <= 100): "))
b = float(input("значення b (1 <= b <= 100): "))

# перевірка на правильність
if not (1 <= a <= 100 and 1 <= b <= 100):
    print("помилка")
else:
    # обчислення за вказаними умовами
    if a < b:
        X = a / b + 5
    elif a == b:
        X = -5
    else:
        X = (a ** 2 - b) / b
# виводим результат
    print(f"значення X: {X}")
